# v1.0 validation record

Environment: macOS host, Docker Desktop, PostgreSQL 17.5, Python 3.12, Node 22, local URLs on ports
3001 and 8001. Date: 2026-07-18.

- Backend: Ruff formatting/checks passed; strict MyPy passed; 30 tests passed.
- Frontend: ESLint and strict TypeScript passed; 12 Vitest tests passed; production build passed.
- Browser: eight Playwright desktop/mobile tests passed after dev-server warm-up, including automated
  serious/critical axe scans on landing, directory, and sign-in.
- Docker: database, API, and web healthy; Alembic `20260718_0006 (head)`.
- API: state, city, eligibility, and geographic sorting exercised against PostgreSQL.
- Recovery: timestamped backup and disposable restore succeeded; restored application CLI examined
  all six services.
- Dependencies: npm production audit and Python package audit reported no known vulnerabilities.

The first browser run after rebuilding the development server timed out during route compilation;
the immediate warm run passed all eight tests. Stable v1.0 remains blocked by the unchecked items in
the release checklist.
